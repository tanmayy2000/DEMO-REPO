trigger AccountTriggerHandlerTrigger on Account (before insert,after insert) {
   /* if(trigger.isBefore && trigger.isInsert)
    {
        AccountTriggerHandler.rating(trigger.new);
        AccountTriggerHandler.nameValidation(trigger.new);
    }
    if(trigger.isAfter && trigger.isInsert)
    {
        AccountTriggerHandler.makeAccount(trigger.new);
        AccountTriggerHandler.defaultContact(trigger.new);
        
        AccountTriggerHandler.createopportunity(trigger.new);
    }*/
    
}