trigger ContactTriggerHandlerTrigger on Contact (before insert,after insert,after delete,before update){
    
   /* if(trigger.isAfter && trigger.isInsert)
    {
        ContactTriggerHandler.changeName(trigger.new);
        
        
    }
    if(trigger.isAfter && trigger.isDelete)
    {
        ContactTriggerHandler.deleteAccountLastName(trigger.old);
    }
    if(trigger.isBefore && trigger.isInsert)
    {
        ContactTriggerHandler.contactUserAddress(trigger.new);
        ContactTriggerHandler.addressTransfer(trigger.new);
        ContactTriggerHandler.allowMultipleContacts(trigger.new);
        ContactTriggerHandler.sameCountryError(trigger.new);
    }*/
    
    
    
}