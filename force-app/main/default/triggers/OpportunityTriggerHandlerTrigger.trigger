trigger OpportunityTriggerHandlerTrigger on Opportunity (before update,after insert,before insert,after update) {
  /*  if(trigger.isBefore )
    { if(trigger.isInsert || trigger.isUpdate)
        OpportunityTriggerHandler.alert(trigger.new);
     OpportunityTriggerHandler.stop(trigger.new);
    }
    if(trigger.isAfter && trigger.isInsert)
    {
        OpportunityTriggerHandler.opportunityContact(trigger.new);
    }
    if(trigger.isBefore )
    {
        if(trigger.isInsert)
        {
            OpportunityTriggerHandler.showError(trigger.new);
            OpportunityTriggerHandler.restrict(trigger.new);
        }
    }
    if(trigger.isAfter && trigger.isUpdate)
    {
        OpportunityTriggerHandler.newCaseForOppTrigger(trigger.new);
    }*/
    
}